name: apple_natural_language
description: >-
  Flutter bindings for Apple's NaturalLanguage framework: language identification, tagging, tokenization and embeddings.
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: '>=3.38.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  pigeon: ^29.0.2

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: AppleNaturalLanguagePlugin
        sharedDarwinSource: true
      macos:
        pluginClass: AppleNaturalLanguagePlugin
        sharedDarwinSource: true
