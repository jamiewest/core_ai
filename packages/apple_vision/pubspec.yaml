name: apple_vision
description: >-
  Flutter bindings for Apple's Vision framework: text recognition, detection, classification, segmentation and more.
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: '>=3.38.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  pigeon: ^29.0.2

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: AppleVisionPlugin
        sharedDarwinSource: true
      macos:
        pluginClass: AppleVisionPlugin
        sharedDarwinSource: true
