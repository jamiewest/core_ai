name: apple_sound_analysis
description: >-
  Flutter bindings for Apple's SoundAnalysis framework: on-device sound classification.
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: '>=3.38.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  pigeon: ^29.0.2

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: AppleSoundAnalysisPlugin
        sharedDarwinSource: true
      macos:
        pluginClass: AppleSoundAnalysisPlugin
        sharedDarwinSource: true
